# hermes-sqlbot-adapter

Hermes 进程内插件：通过 SQLBot MCP 提供 `finance-bi` Toolset。

## 工具

- `finance_bi_ask`
- `finance_bi_followup`
- `finance_bi_explain`
- `finance_bi_reset`

模型看不到 SQLBot 用户名、密码、`access_token`、`chat_id`。

## 依赖

见 `requirements.txt`（httpx / sqlglot / PyYAML）。
